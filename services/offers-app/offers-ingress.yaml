apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: offers-gateway
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: offers
spec:
  hosts:
  - "*"
  gateways:
  - offers-gateway
  http:
  - match:
    - uri:
        exact: /offers
    route:
    - destination:
        host: offers
        port:
          number: 80